/** Default number of issues loaded per paginated section (backlog, kanban, lists). */
export const DEFAULT_PAGE_SIZE = 15

/** Maximum page size the API accepts via the page_size query param. */
export const MAX_PAGE_SIZE = 100
